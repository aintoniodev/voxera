# autoresearch + model env (local)
# Build artifacts (acceptance pipeline + project-local temp)
# pi agent local state
# Python
# SwarmForge runtime state (per-machine)
*.egg-info/
*.py[cod]
.auto/
.pi/
.pytest_cache/
.swarmforge/
.venv/
.worktrees/
__pycache__/
acceptance/generated/
build/
tmp/

# model files (downloaded)
models/
.venv-re21/

# Tauri desktop build (voxera-desktop)
voxera-desktop/node_modules/
voxera-desktop/dist/
voxera-desktop/src-tauri/target/

# benchmark v2 tooling (scripts SI, artefactos NO)
.auto/
!.auto/
.auto/*
!.auto/v2/
.auto/v2/*
!.auto/v2/*.py

# demo video (Remotion)
voxera-demo/node_modules/
voxera-demo/out/

# secretos / credenciales (defensa en profundidad)
.env
.env.*
*.pem
*.key
*.p12
*.pfx
.netrc
credentials.json

# vídeos personales + deliverables de fase 3 (no van al repo)
media/videos/
